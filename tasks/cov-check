#!/bin/bash

npx c8 \
    --check-coverage \
    --all \
    --include "$SRC" \
    --reporter=text-summary \
    --reporter=lcov \
    --branches 80 \
    --lines 40 \
    --functions 60 \
    --statements 40 \
    ./tasks/test
